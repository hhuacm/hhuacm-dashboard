# Local development defaults to http://localhost:3000 when this is omitted.
# In Docker production, set this to the API server service URL, for example:
# SERVER_INTERNAL_URL=http://server:3000
SERVER_INTERNAL_URL=http://localhost:3000

# next start reads PORT directly. next dev uses the package script port.
PORT=3000
